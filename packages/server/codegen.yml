schema: http://localhost:4000/graphql
overwrite: true
generates:
  ./src/types.ts:
    config:
      contextType: ./context#IContext
    plugins:
      - typescript-common
      - typescript-server
      - typescript-resolvers
